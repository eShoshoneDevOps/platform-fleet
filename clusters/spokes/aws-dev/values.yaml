clusterName: aws-dev
environment: dev
cloud: aws
region: us-east-1

cilium:
  k8sServiceHost: ""    # replace with EKS API server endpoint

prometheus:
  prometheus:
    prometheusSpec:
      replicas: 1
      retention: 1h
      retentionSize: "4GB"
